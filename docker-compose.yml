services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    environment:
      - CADENCE_DB_PATH=/app/var/cadence_log.db
    volumes:
      - ./data:/app/data:ro
      - cadence-db:/app/var
    develop:
      watch:
        - path: ./backend/app
          action: sync
          target: /app/app

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  cadence-db:
