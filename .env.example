# Copy to .env (which is gitignored) and fill in real values.

# OpenRouter API key — the model layer reads this; never hardcode it.
OPENROUTER_API_KEY=

# Optional: when set, the log repository uses Postgres; otherwise local SQLite.
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/cadence
